# Default: SQLite for zero-config Postman tests
DATABASE_URL=sqlite:///./day16_local.db

# Docker Postgres (see docker-compose.yml): uncomment after `docker compose up -d`
# DATABASE_URL=postgresql+psycopg://day16:day16@localhost:5433/day16

CHROMA_PATH=./chroma_data
